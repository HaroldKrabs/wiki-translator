#!/usr/bin/env python3
# -*- coding: utf-8 -*-

from __future__ import annotations

from pathlib import Path

BASE_DIR = Path(__file__).resolve().parent
INPUT_DIR = BASE_DIR / "input"
OUTPUT_DIR = BASE_DIR / "translated"
PROGRESS_DIR = BASE_DIR / ".progress"
LOG_DIR = BASE_DIR / "logs"
LOG_FILE = LOG_DIR / "translator.log"
ERROR_LOG_FILE = LOG_DIR / "errors.log"
WARNINGS_FILE = BASE_DIR / "warnings_report.txt"
LIST_FILE_NAME = "0010010.txt"

MODEL_NAME = "gemini-3.5-flash-lite"
MIN_REQUEST_INTERVAL = 2.6
MAX_DAILY_REQUESTS = 900
MAX_RETRIES = 7
TEMPERATURE = 0.08
REVIEW_TEMPERATURE = 0.15
MAX_WORKERS = 3
MAX_PARAGRAPHS_PER_CHUNK = 5
MAX_CHARS_PER_CHUNK = 9500
MAX_OUTPUT_TOKENS = 16384
REVIEW_MAX_CHARS = 9000
REVIEW_OVERLAP_CHARS = 400

RATE_LIMIT_BASE_WAIT = 12
RATE_LIMIT_MAX_WAIT = 180
NETWORK_BASE_WAIT = 8
NETWORK_MAX_WAIT = 90
KEY_COOLDOWN_ON_429 = 50

WIKI_MIN_INTERVAL = 0.85
WIKI_MAX_RETRIES = 7
WIKI_BASE_WAIT = 6
WIKI_MAX_WAIT = 120

WIKI_API = "https://en.wikipedia.org/w/api.php"
WIKI_USER_AGENT = "WikiTranslatorBot/5.0 (Persian Wikipedia translator; contact: local)"
REQUEST_TIMEOUT = 40
BATCH_SIZE = 40
DEFAULT_MAX_ARTICLES = 50

SYSTEM_PROMPT = """
تو مترجم حرفه‌ای و متخصص ویکی‌پدیای فارسی هستی.
فقط متن ویکی‌کد انگلیسی را به فارسی روان، رسمی و دانشنامه‌ای ترجمه کن.

قوانین اجباری (نقض هر کدام ممنوع است):

۱. پیوندهای داخلی (مهم‌ترین قانون):
   - هر [[Target]] و [[Target|Display]] را حتماً حفظ کن.
   - هدف لینک (Target) را هرگز ترجمه، تغییر، حذف یا به متن ساده تبدیل نکن.
   - فقط بخش Display (اگر وجود دارد) را در صورت نیاز به فارسی ترجمه کن.
   - مثال درست: [[Time]] → [[Time]] یا [[Time|تایم]]
   - مثال درست: [[Varanasi|Varanasi]] → [[Varanasi|واراناسی]]
   - مثال غلط: تبدیل [[Time]] به «تایم» بدون براکت.
   - اگر در پیام لیست پیوندهای اجباری آمده، همه آن‌ها را در خروجی نگه دار.

۲. ساختار ویکی و محتوای محافظت‌شده:
   - توکن‌های به شکل ⟦WTP0⟧ یا ⟦WTP12⟧ را هرگز تغییر نده، حذف نکن و ترجمه نکن؛ عیناً در همان محل نگه دار.
   - تمام <ref>...</ref> و <ref name="..." /> و محتوای کامل داخلشان را عیناً نگه دار (هیچ کلمه‌ای از داخل ref را حذف یا خلاصه نکن).
   - قالب‌های {{...}} را حفظ کن (فقط متن قابل‌ترجمه داخل پارامترهای ساده را ترجمه کن؛ قالب‌های cite و sfn را دست نزن).
   - تیترها (== Title ==) را ترجمه کن ولی علامت‌های == را دقیقاً حفظ کن.
   - ترتیب پاراگراف‌ها و خطوط خالی را حفظ کن.
   - خطوط Category: و [[Category:...]] و [[رده:...]] را اگر دیدی همان‌طور نگه دار.

۳. حروف و متون غیرلاتین:
   - حروف چینی، ژاپنی، کره‌ای، چو هان (Chữ Hán)، چو نوم و هر نویسهٔ بومی دیگر که در متن اصلی هست را عیناً نگه دار و حذف نکن.
   - نام‌های بومی و نقل‌قول‌های اصلی به زبان مبدأ را حفظ کن.

۴. کیفیت ترجمه:
   - فارسی طبیعی، رسمی و مطابق سبک ویکی‌پدیای فارسی.
   - از گیومه فارسی «» استفاده کن.
   - نام‌های خاص را یکدست بنویس.

۵. خروجی:
   - فقط متن ترجمه‌شده را برگردان. هیچ توضیح اضافی ننویس.
""".strip()

TITLE_TRANSLATE_PROMPT = """
تو مترجم حرفه‌ای عناوین مقالات ویکی‌پدیا هستی.
عنوان انگلیسی زیر را به فارسی روان، رسمی و مطابق عرف رایج در ویکی‌پدیای فارسی ترجمه کن.
فقط خود عنوان ترجمه‌شده را برگردان. هیچ توضیح یا علامت اضافی ننویس.
""".strip()

REVIEW_PROMPT = """
تو ویراستار ارشد ویکی‌پدیای فارسی هستی.
متن زیر ترجمه‌ای از ویکی‌کد انگلیسی است که نیاز به بازبینی نهایی دارد.

وظایف تو (فقط همین‌ها):
۱. فارسی را روان، رسمی و دانشنامه‌ای کن؛ جملات خشک یا تحت‌اللفظی را طبیعی کن.
۲. یکدستی واژگان و نام‌های خاص را در کل تکه حفظ کن.
۳. ساختار ویکی را ۱۰۰٪ حفظ کن:
   - همه [[Target]] و [[Target|Display]] را دست نزن (نه حذف، نه ترجمه Target).
   - همه <ref>...</ref> و محتوای کامل داخلشان، {{sfn}}، {{cite ...}} و تیترهای == را عیناً نگه دار.
   - توکن‌های ⟦WTP0⟧ و مشابه را اگر دیدی تغییر نده.
   - حروف چینی، چو هان، چو نوم و سایر نویسه‌های غیرلاتین را حذف نکن.
۴. هیچ بخش از محتوا را حذف یا خلاصه نکن؛ طول متن نباید به‌طور محسوس کوتاه شود.
۵. خطوط Category / رده را اگر دیدی دست نزن (سیستم بعداً تطبیق می‌دهد).
۶. فقط متن بازبینی‌شده را برگردان؛ هیچ توضیح یا مقدمه ننویس.

اگر «متن قبلی (برای انسجام)» داده شده، فقط برای حفظ لحن و واژگان از آن استفاده کن و آن را در خروجی تکرار نکن.
""".strip()

PERSIAN_ALPHABET = "آابپتثجچحخدذرزژسشصضطظعغفقکگلمنوهی"
